{
  "name": "honojs-api",
  "version": "1.0.0",
  "description": "REST API with Bun, Hono.js, and Prisma ORM",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "@prisma/client": "^5.8.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^5.8.0"
  }
}
